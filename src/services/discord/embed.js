const { MessageEmbed } = require('discord.js')

module.exports = MessageEmbed
/*module.exports = {
  createEmbed: () => {
    return new MessageEmbed()
  },
  embedSetTitle: (embed, title) => embed.setTitle(title),
  embedSetAuthor: (embed, author) => embed.setAuthor(author),
  embedSetColor: (embed, color) => embed.setColor(color),
  embedSetDescription: (embed, description) => embed.setDescription(description),
  embedSetFooter: (embed, footer) => embed.setFooter(footer),
  embedSetUrl: (embed, url) => embed.setUrl(url),
  embedSetThumbnail: (embed, url) => embed.setThumbnail(url),
  embedAddField: (embed, fieldName, fieldValue) => embed.addField(fieldName, fieldValue),
}*/
